<!--pages/page/friend.wxml-->
<scroll-view class="friend-container" bindscroll="scrollUp">
  <friendShow comment="{{comment}}" count="{{count}}"></friendShow>
  <view class='postComment {{hidden?"down":""}}' wx:if="{{scrollUp}}">
    +
  </view>
</scroll-view>
